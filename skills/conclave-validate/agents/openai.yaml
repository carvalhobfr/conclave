interface:
  display_name: "Conclave Validate"
  short_description: "Validate changes against repository evidence"
  default_prompt: "Use $conclave-validate to validate this branch against its objective and show the evidence-backed verdict."

policy:
  allow_implicit_invocation: true
